import pandas as pd
import numpy as np

def v_split(df, col_list):
    df = df.copy()
    
    with warning.catch_warnings():
        warnings.simplefilter('ignore')
        scaler, encoder, train, test = 
    